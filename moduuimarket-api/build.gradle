bootJar { enabled = true }
jar { enabled = false }

dependencies {
    implementation project(':moduuimarket-domain')

    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework:spring-tx'
}